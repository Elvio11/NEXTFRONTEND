# Built-in OpenFang Hand: Collector
name = "collector"
description = "Pings all 5 servers for basic health metrics."
llm_provider = "groq"

[schedule]
cron = "0 5 * * *" # Runs daily at 5:00 AM IST

[tools]
enabled = ["http_get", "notify"]