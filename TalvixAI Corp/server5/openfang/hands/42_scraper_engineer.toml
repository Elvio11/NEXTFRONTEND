# Built-in OpenFang Hand: Browser
name = "browser"
description = "Automates visual frontend QA checks via Playwright to detect free/paid tier UI gate breakages."
llm_provider = "groq"

[schedule]
cron = "30 5 * * *" # Runs daily at 5:30 AM IST

[tools]
enabled = ["browser_automation", "notify"]